# Phase 07: Fine-tuning & Customization
# Copy this file to .env and fill in your real keys: cp .env.example .env
# (Or, in Google Colab, set these as os.environ[...] instead — see README step 4)

OPENAI_API_KEY=sk-...     # for LLM-as-judge evaluation
HF_TOKEN=hf_...            # optional, for pushing the fine-tuned model to the Hub
