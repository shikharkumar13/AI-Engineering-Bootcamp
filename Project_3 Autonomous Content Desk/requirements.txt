# Project 3: Autonomous Content Desk
# Union of Phase 6 + Phase 8's dependencies (this project imports Phase 6's
# code directly and follows Phase 8's production-shell pattern; the Phase 7
# fine-tuned model is called over HTTP, not imported — see crew.py)
# pip install -r requirements.txt

crewai>=0.70.0
crewai-tools>=0.12.0

langchain>=0.3.0
langchain-openai>=0.2.0

openai>=1.40.0
pydantic>=2.0.0
tavily-python>=0.5.0

fastapi>=0.110.0
uvicorn[standard]>=0.30.0
slowapi>=0.1.9
httpx>=0.27.0

python-dotenv>=1.0.0
