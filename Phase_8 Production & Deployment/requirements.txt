# Phase 08: Production & Deployment
# pip install -r requirements.txt

# Backend
fastapi>=0.110.0
uvicorn[standard]>=0.30.0
slowapi>=0.1.9
pydantic>=2.0.0
python-dotenv>=1.0.0
# Required by main.py's /index endpoint (UploadFile/File()) — FastAPI raises
# RuntimeError at startup for ALL routes if this is missing, not just /index.
python-multipart>=0.0.9

# AI / RAG
openai>=1.40.0
chromadb>=0.5.0
# Pinned below 0.4: ragas's internal imports (see below) still reach into
# langchain_community.chat_models.vertexai, which 0.4.x removed (moved to the
# separate langchain_classic package) — `pip install -r requirements.txt` with
# an unpinned langchain-community pulls a version ragas can't import at all.
langchain-community>=0.3.0,<0.4.0
langchain-text-splitters>=0.3.0
pypdf>=4.0.0
beautifulsoup4>=4.12.0

# Evaluation
ragas>=0.1.0
datasets>=2.20.0

# Observability
# >=3.0.0: observability.py uses the OTel-based API (top-level observe/get_client),
# introduced when langfuse.decorators (the v2 API) was removed.
langfuse>=3.0.0

# Frontend
streamlit>=1.38.0
httpx>=0.27.0
